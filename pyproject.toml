[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "Muil_ArkUI-UT-Agent"
version = "0.1.0"
description = "Repository-aware Code Agent for OpenHarmony ArkUI Ace Engine"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

